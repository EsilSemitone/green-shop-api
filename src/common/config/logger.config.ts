export const LOGGER_CONFIG = {
    transport: {
        target: 'pino-pretty',
    },
    // formatters: {
    //     bindings: (bindings) => {
    //         return {
    //           pid: bindings.pid,

    //         };
    //       },
    // }
};
